# Guardian V7 策略逻辑文档

## 一、核心策略概述

Guardian 是一个 **纯 Maker（挂单方）** 自动化做市机器人，运行在 Polymarket CLOB（中央限价订单簿）交易平台上。

**核心目的：挂单提供流动性以获取平台返利奖励。** Polymarket 当前有挂单返利活动——只要在订单簿上挂限价单提供流动性，就能获得奖励。因此机器人只做纯 Maker 操作：挂限价买单提供流动性赚取奖励，成交后通过定时扫描持仓自动挂卖单平仓，始终保持资金在提供流动性的状态。

**只挂单、不吃单**，机器人从不主动吃掉订单簿上已有的订单。如果挂单价格设置不当（跨价），会立即匹配成为 Taker，既损失手续费又白费一次挂单机会。因此机器人通过监控订单簿实时调整价位来避免跨价。

---

## 二、进入策略：如何挂买单

### 2.1 挂单价位选择

机器人监控目标市场订单簿的**买盘（Bids）**方向，按价格从高到低排序：

```
买盘（Bids）:
  档位 1（best_bid）: 0.51  ← 市场上最高买价
  档位 2:            0.50
  档位 3:            0.49  ← 我们的目标档位（maker_rank=3）
  档位 4:            0.48
  ...
```

机器人选择第 **maker_rank** 档的价格作为挂单价。默认 `maker_rank=3`，即挂在买盘第 3 档。

**为什么不是第 1 档？**
- 第 1 档（best_bid）竞争最激烈，成交快但利润薄
- 第 3 档价格更低，买入成本更低，利润空间更大
- 但也意味着成交速度更慢——越低的买价越难被匹配

档位选择本质上是在**成交速度 vs 利润空间**之间的权衡。

### 2.2 挂单量

每次挂单使用固定的数量，默认 `maker_size=50`（50 USDC 等值的代币）。这个量需要在"足够大以获取可观利润"和"足够小以快速成交"之间平衡。

### 2.3 价格对齐

订单簿的最小价格变动单位是 `tick_size`（默认 0.01）。挂单前，目标价格会四舍五入对齐到 tick_size 的倍数，确保 API 接受订单。

### 2.4 市场来源

机器人通过两种方式获取需要挂单的市场：

**方式一：CSV 文件同步（主动）**

配置 `MARKET_FILE` 指向 screener 输出的 CSV 文件，Guardian 每 30s 读取文件，为 YES 和 NO 两个 token 各自创建 Actor 并自动启动挂单周期。这是主要市场来源。

Guardian 跟踪哪些 token_id 来自 CSV 文件（`_file_managed_ids`）。当某个 token_id 在 CSV 中不再出现时（screener 筛选条件变化导致市场被移除），Guardian 会停止该市场的 Actor：取消活跃订单、停止监控并从管理列表中移除。CSV 文件同步的移除机制仅对来自方式一的市场生效——来自方式二的市场不受 CSV 变化影响。

**方式二：已有订单接管（被动）**

每 30s 执行 `discover()` 扫描已有买单，发现未被管理的订单时创建 Actor 接管。用于重启后恢复之前已在交易所挂着的订单。

两种方式并行，token_id 自动去重。

---

## 三、退出策略：定时扫描持仓挂卖单

卖出不依赖 WebSocket 事件驱动，而是通过**定时轮询**的方式处理。

每 120 秒执行一次 `check_positions()`，流程如下：

```
1. 查询全部 open_orders，找出所有已有卖单对应的 token_id
2. 通过 Data API 查询当前所有持仓
3. 对每个持仓：
   a. 跳过已有卖单挂着的持仓（避免重复挂单）
   b. 跳过正在卖出中的持仓（并发保护）
   c. 查询链上余额确认持仓量
   d. 按持仓成本均价，挂一个同价同量的限价卖单（GTC）
```

**为什么用轮询而不是事件驱动？**

- 简单可靠：不依赖 WS 事件链路（MATCHED → CONFIRMED → 卖出），减少中间环节
- 天然去重：查询已有卖单后再决定是否补挂，不会重复
- 自愈能力强：重启、WS 断线重连、事件丢失等场景下，最多 120s 后自动补挂
- 卖出本身也是限价单，同样在提供流动性赚取返利奖励

---

## 四、订单被撤单后的处理

只保留一种撤单路径：**系统主动撤单**。

### 触发条件

best_bid（第 1 档买价）发生变化或 WS 重连。

### 处理流程

```
检测到需要撤单 → CANCELING → 调用撤单 API
  ├─ 成功 → NO_ORDER → 冷却 120s → 重新挂单
  └─ 失败 → 保持 RESTING（订单仍在交易所存活）
```

### 审计纠偏

`audit()` 每 120s 通过 `POST /books` 批量查询所有买单的 best_bid，与挂单价比对。若 `price >= best_bid`（超价），通过执行层直接撤单并通知 Actor 进入冷却重挂。

### 不再处理的情况

- **外部撤单**（WS CANCELLATION 事件）：不再监听，仅记录日志
- **下单中行情变化**：不再设 `_pending_reeval`，由 audit 兜底纠偏
- **部分成交**：订单仍在簿上则保持 RESTING，由 audit 检测实际状态

---

## 五、市场状态机

每个被守护的市场由一个 `MarketState` 管理，所有状态由主线程统一处理。

### 状态转换

```
                    ┌──────────┐
           ┌───────→│ NO_ORDER │←──────────┐
           │        └─────┬────┘           │
           │    有足够报价档位│             │ 冷却到期
           │        ┌─────▼────┐           │
           │        │ PLACING  │           │
           │        └─────┬────┘           │
           │      下单成功  │             │
           │        ┌─────▼────┐ 撤单成功   │
           │        │ RESTING  ├───────────┤
           │        └──┬───┬──┘           │
           │   成交匹配  │   │ best_bid变化 │
           │   (不变)   │   │ target变化   │
           │           │   │              │
           │        ┌──▼───▼──┐           │
           │        │CANCELING│           │
           │    ┌───┤ (异步)  ├───┐       │
           │    │   └─────────┘   │       │
           │    │ 成功            │ 失败  │
           │    ▼                 ▼       │
           │  ┌─────┐        ┌────────┐   │
           │  │STOP │        │RESTING │   │
           │  │PED  │        │(保持)   │   │
           │  └─────┘        └────────┘   │
           │                              │
           │                        ┌─────▼────┐
           └────────────────────────│ COOLING  │
                                    │ 120s→重挂 │
                                    └──────────┘
```

### 各状态含义

| 状态 | 含义 | 允许的动作 |
|------|------|-----------|
| NO_ORDER | 当前无订单挂在交易所 | 收到订单簿数据后挂单；或等待冷却 |
| PLACING | 正在向交易所提交订单 | 等待下单结果（异步） |
| RESTING | 订单已挂在订单簿上，等待成交 | 监控 best_bid 变化，随时准备撤单重挂 |
| CANCELING | 正在撤销当前订单 | 等待撤单结果（异步） |
| COOLING | 撤单后等待冷却期 | 120s 后重新挂单 |
| STOPPED | 已停止，不再管理该市场 | 无 |

---

## 六、集中式状态管理

V7.4 起，所有市场状态集中在主线程的 `Dict[str, MarketState]` 中，不再使用独立 Actor 线程和事件队列。

**状态变更机制：**

| 场景 | 实现 |
|------|------|
| best_bid 变化检测 | `_poll_best_bids()` 每 3s 直接读写 MarketState |
| 冷却到期→挂单 | `_check_cooldowns()` 每秒检查 `cooldown_until` |
| 异步下单/撤单结果 | `_check_pending_ops()` 每秒轮询 `Future.done()` |
| 审计纠偏 | `audit()` 每 120s 直接遍历 `_markets` |

**关键设计决策：**
1. 主线程独占 MarketState，无需锁
2. 冷却不再用 Timer，改为时间戳 + 主循环检查
3. 异步结果不通过回调线程回传，回调只 `set_result()`，主循环轮询

---

## 七、数据反馈回路

### 7.1 订单簿数据来源

挂单前通过 REST API `GET /book?token_id=...` 实时获取当前买盘数据。

best_bid 变化检测：每 3s 通过 `POST /books` 批量查询所有市场的 best_bid，与 MarketState 缓存值对比。变化时直接触发撤单+冷却+重挂。

### 7.2 best_bid 变化检测

best_bid 是市场上最高的买价。当 best_bid 变化时：
- **首次收到**：仅记录，不触发动作。因为此时还没有历史数据可以判断"变化"。
- **RESTING 状态**：说明行情变了，撤单冷却后重挂。
- **PLACING 状态**：不处理，由 `audit()` 在下一次审计时纠偏。
- **NO_ORDER 状态**：行情活跃，启动冷却准备挂单。

---

## 八、心跳保活机制

Polymarket 要求通过 REST API 每约 10 秒发送一次心跳（`POST /v1/heartbeats`），否则**所有挂单会在约 15 秒后被交易所自动取消**。

### 心跳协议

```
首次: POST {"heartbeat_id": ""}            → 200 {"heartbeat_id": "abc123"}
后续: POST {"heartbeat_id": "abc123"}      → 200 {"heartbeat_id": "def456"}
过期: POST {"heartbeat_id": "expired"}    → 400 {"heartbeat_id": "correct", ...}
```

每次请求包含上次返回的 heartbeat_id，服务器返回新的 ID。如果 ID 过期，返回 400 并提供正确 ID，更新后重试。

### 容错策略

1. 优先使用 SDK 的 `post_heartbeat()` 方法
2. 如果 SDK 调用失败，从错误消息中正则提取 heartbeat_id，然后回退到 L2 认证头的裸 HTTP 请求
3. 连续失败 ≥3 次触发 CRITICAL 级别告警

### 生命周期

- **启动**：心跳最先启动（在任何订单操作之前）
- **关闭**：心跳最后停止（在所有订单取消完毕之后）

这样确保从启动到关闭的整个运行窗口内，订单始终被心跳保护。

---

## 九、定时任务调度

主循环以 1 秒为粒度运行，检查各定时任务是否到期：

| 任务 | 间隔 | 职责 |
|------|------|------|
| discover() | 30s | 从订单接管 + CSV 文件同步新增/移除市场；清理 STOPPED 状态 Actor |
| _poll_best_bids() | 3s | 批量查询 `POST /books`，检测 best_bid 变化推送给 Actor |
| audit() | 120s | 批量 `POST /books` 查 best_bid，纠偏超价订单；检测订单丢失；状态卡死重置 |
| check_positions() | 120s | 扫描持仓 → 补挂限价卖单（唯一卖出路径） |
| _prune_caches() | 300s | 清理过期缓存，防止内存泄漏 |

---

## 十、限流与幂等保护

所有 REST 写操作（下单、撤单）通过 ExecutionLayer 统一执行：

### 全局限流

两次写操作之间至少间隔 `exec_interval`（默认 0.2s）。所有操作共享同一个限流器，包括下单、撤单、卖单。

### 幂等-下单

同一 `asset_id + price` 组合在 300 秒内不会重复下单。如果第一次下单失败（包括 API 返回成功但无 order_id），token 会被清除，允许重试。

### 幂等-撤单

同一 `order_id` 不会重复撤单。如果撤单失败，token 被清除允许重试；如果撤单成功，token 保留以防止重复。

### 系统撤单标记

撤单操作通过 `CANCEL_DONE` 内部事件闭环处理，不依赖 WebSocket CANCELLATION 确认。

---

## 十一、WebSocket 管理

### 单频道架构

仅保留**用户频道**（`/ws/user`）：接收 trade 事件（用于日志记录）和 order 事件（日志记录）。

市场数据（best_bid）不再通过 WebSocket 获取，改为每 3s 批量轮询 `POST /books` REST API。

### 重连策略

用户频道由独立线程管理，线程内 while 循环处理"连接→断开→重连"。断线后等待 `ws_reconnect_delay`（默认 5s）重连。

---

## 十二、缓存与内存管理

### 三类缓存

| 缓存 | 用途 | 清理策略 |
|------|------|---------|
| `_ob_cache` | best_bid 查询结果缓存，减少 REST API 调用 | TTL 过期清理（2×cache_ttl 或 60s） |
| `_market_info` | 市场名称/标题缓存 | 超过 cache_max_size 时淘汰一半 |
| `_processed_trades` | 已处理的 trade ID，防止重复处理 | 超过 trade_max_size 时按时间戳淘汰最旧的 |

所有缓存读写由 `_cache_lock`（RLock）保护，确保多线程安全。

---

## 十三、优雅关闭流程

系统收到 `Ctrl+C`（SIGINT/SIGTERM）后的关闭顺序：

```
1. 取消所有活跃订单（等待 cancel_timeout 每条订单 10s）
2. force_stop 所有 Actor（等待最多 5s）
3. 断开 WebSocket 连接
4. 关闭线程池（wait=True，等待所有 pending 任务完成）
5. 停止心跳
```

关闭顺序的设计原则：
- 先取消订单再停心跳：确保订单取消在心跳保护期内完成
- 先停 Actor 再断 WS：避免 Actor 在 WS 断开后收到无用事件
- 先断 WS 再关线程池：避免线程池中的任务依赖 WS

---

## 十四、启动顺序

```
1. heartbeat.start()          — 心跳最先，在发现任何订单之前
2. ws_manager.start()          — 启动 WS 用户频道
3. 用户频道认证 + 订阅          — 获取 trade/order 事件
4. discover()                  — 接管已有买单 + CSV 文件同步新市场
5. 主循环                      — 五定时任务轮转
```

---

## 十五、日志体系

| 日志文件 | 内容 |
|----------|------|
| `data/guardian.log` | 全部运行日志（控制台同步输出） |
| `data/trades.log` | 买卖配对记录（买入成交 + 卖出挂单/持仓卖出） |
| `data/cancels.log` | 撤单记录（区分系统撤单/外部撤单） |


---

## 十六、关键设计原则

1. **保守优先**：无法区分"交易所自动取消"和"用户手动取消"时，走重挂路径而非放弃，由多周期检测兜底
2. **异步不阻塞**：所有 REST 操作通过线程池异步执行，Actor 事件循环不等待 IO
3. **状态变更串行化**：所有 Actor 状态变更在 Actor 自己的线程内执行，通过内部事件队列回传异步结果
4. **防御式编程**：所有外部输入（WS 事件、API 响应）经过安全类型转换（safe_float/safe_decimal），失败返回默认值而非抛异常
5. **指令文档一致**：代码默认值、测试断言、文档描述三者必须对齐
