# Polymarket 策略项目 — 开发与部署模板

> **用途**：新开一个 Polymarket Python 策略项目时，照这份文档搭好骨架（GitHub 仓库、本地开发环境、服务器部署、运维脚本），然后再往里面填策略代码。
>
> 本文档只覆盖**与策略无关的通用流程**。策略代码的写法取决于你的具体策略，请参考 [Polymarket 官方 Python SDK 文档](https://docs.polymarket.com/getting-started/python) 和 [API 文档](https://docs.polymarket.com/getting-started/api)。

---

## 目录

1. [创建仓库](#1-创建仓库)
2. [本地开发环境](#2-本地开发环境)
3. [开发工作流](#3-开发工作流)
4. [服务器部署](#4-服务器部署)
5. [运维脚本](#5-运维脚本)
6. [定时任务（cron）](#6-定时任务cron)
7. [多账户运行](#7-多账户运行)
8. [日志管理](#8-日志管理)
9. [安全检查清单](#9-安全检查清单)
10. [命令速查表](#10-命令速查表)

---

## 1. 创建仓库

### 1.1 在 GitHub 上新建

- 建议 **Public**（方便服务器 `git clone` 无需额外配置 SSH key）
- 不要勾选 "Initialize with README"
- 仓库名随意，纯小写 + 连字符/下划线均可

### 1.2 本地初始化 + 最早两个文件

```powershell
# Windows 本地
cd 你的开发目录
mkdir 项目文件夹
cd 项目文件夹
git init
git remote add origin https://github.com/你的用户名/仓库名.git
```

**第一个文件：`.gitignore`**（防私钥泄露，所有项目通用）

```
# Python
__pycache__/
*.py[cod]
*.so
.Python

# Virtual environment
venv/
env/

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# 🔒 安全关键 — 含私钥的文件绝对不能进 git
data/
*.log
.env
.env.*
```

**第二个文件：`.gitattributes`**（Windows 开发机必须——防止 shell 脚本换行符被转成 CRLF，到 Linux 上跑不了）

```
*.sh text eol=lf
```

### 1.3 首次提交

```powershell
git add .gitignore .gitattributes
git commit -m "初始化仓库"
git push -u origin main
```

---

## 2. 本地开发环境

### 2.1 Python 与依赖

```powershell
# 创建虚拟环境
python -m venv venv
venv\Scripts\activate        # Windows
# source venv/bin/activate   # Linux/Mac

# 安装 SDK（官方统一包，覆盖 CLOB / Gamma / Data API 全部接口）
pip install polymarket-client

# 其他常用依赖
pip install python-dotenv websocket-client requests

# 冻结依赖清单
pip freeze > requirements.txt
```

> **官方 SDK 是什么**：[`polymarket-client`](https://pypi.org/project/polymarket-client/)（GitHub: [Polymarket/py-sdk](https://github.com/Polymarket/py-sdk)）。
> 提供 `AsyncPublicClient`（公开数据）、`AsyncSecureClient`（交易/账户）、实时订阅等统一接口。
> 同步版也有（`PublicClient` / `SecureClient`），但实时订阅只支持异步版。
> 最新的认证方式、API 用法以官方文档为准：[docs.polymarket.com/getting-started/python](https://docs.polymarket.com/getting-started/python)。

### 2.2 凭据文件

在项目根目录创建 `.env.bot1`（**不进 git，已在 .gitignore 里**）：

```
SIGNER_PRIVATE_KEY=0x...                  # 签名者私钥
POLYMARKET_WALLET_ADDRESS=0x...           # 账户钱包地址
POLYMARKET_RELAYER_API_KEY=...            # Relayer API key（免 gas 交易）
POLYMARKET_RELAYER_API_KEY_ADDRESS=...    # Relayer API key 地址
```

> **官方文档的认证方式**（2026-08 最新）：用 `SecureClient.create(private_key=..., wallet=..., api_key=RelayerApiKey(...))`。
> Relayer API key 在 polymarket.com → Settings → API Keys → Relayer API Keys 创建。
> 详见 [Wallets and Authentication](https://docs.polymarket.com/trading/wallets-auth)。

---

## 3. 开发工作流

### 3.1 核心原则

**每完成一个小改动就提交推送，不要攒一大堆再一次提交。**

```
理解需求 → 改代码 → 本地验证（语法/测试）→ git commit → git push
```

### 3.2 日常操作

```powershell
# 改完代码后
git add 改动的文件1 改动的文件2 ...
git status --short          # 确认没误纳 .env 或临时文件
git commit -m "简述改动"
git push
```

> 不要用 `git add .`——容易把 `.env` 或 `data/` 里的东西扫进去。

### 3.3 提交信息

```
简短描述（一行）

- 具体改了什么
- 影响范围（选填）
```

### 3.4 为什么不攒着

- GitHub 是唯一持久化备份，本地崩了没 push 的代码就丢了
- 服务器靠 `git pull` 同步，push 了才能部署
- 小步提交方便回溯"哪个改动引入了问题"

---

## 4. 服务器部署

> **同一台服务器可以跑多个项目，互不冲突。** 每个项目是独立的目录（如 `/root/project_a/`、`/root/project_b/`），各自的 `venv/`、凭据、日志、screen 会话全在各自目录下，不会互相影响。**只要先 `cd` 进对应项目目录再操作即可。**

### 4.1 首次环境准备

```bash
ssh root@服务器IP

# 基础工具（sudo apt 和 python3-venv 装一次就够了，新项目无需重复）
# 如果服务器上已装过可跳过
sudo apt update && sudo apt install -y python3 python3-venv python3-pip git screen

# 克隆到 /root 下的独立目录（与旧项目平级，各管各的）
cd /root
git clone https://github.com/你的用户名/仓库名.git
cd 仓库名

# 创建 venv + 装依赖（这个 venv/ 是本项目私有的）
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
```

### 4.2 传凭据

```powershell
# 本地 Windows PowerShell
scp .env.bot1 root@服务器IP:/root/仓库名/
```

```bash
# 服务器上
chmod 600 /root/仓库名/.env.bot1
```

### 4.3 首次前台验证

```bash
cd /root/仓库名
source venv/bin/activate
python main.py bot1
# 观察启动日志正常 → Ctrl+C 停掉
```

### 4.4 后台托管（screen）

```bash
screen -S bot1
cd /root/仓库名 && source venv/bin/activate && python main.py bot1
# 确认正常 → Ctrl+A 再按 D（挂后台，断开 SSH 也不影响运行）
```

常用 screen 操作：

| 操作 | 按键/命令 |
|------|----------|
| 挂后台 | `Ctrl+A` 再按 `D` |
| 查看列表 | `screen -ls` |
| 进入会话 | `screen -r bot1` |
| 退出并关闭 | 在 screen 内 `exit` |

---

## 5. 运维脚本

以下两个脚本是**策略无关的通用工具**——只要你的程序通过 `SIGTERM` 优雅退出，就能直接用。

> ⚠️ **多项目共存的关键**：同一台服务器跑多个项目时，**每个项目必须用自己 venv 的完整路径做进程匹配**。
> 如果两个项目都用 `pgrep -f "python main.py bot1"`，会匹配到对方的进程 → 误杀。
> 解决方案：启动时用 `venv/bin/python` 完整路径，pgrep 匹配时也要加上本项目路径前缀。
> 这就是下面脚本里 `WORKDIR` 变量和 `${WORKDIR}/venv/bin/python` 的用意。

### 5.1 start_bot.sh

```bash
#!/bin/bash
# 启动指定实例。用法: ./start_bot.sh [实例名]  默认 bot1
# 多项目共存：每项目的 WORKDIR 不同，pgrep 只匹配本项目进程，不会误判/误杀其他项目。
set -u

INSTANCE="${1:-bot1}"
WORKDIR="/root/仓库名"          # ← 改成你的实际路径

# 查重：用本项目 venv 完整路径匹配，只认本项目进程
if pgrep -f "${WORKDIR}/venv/bin/python.*main.py ${INSTANCE}\$" >/dev/null; then
    echo "[start_bot $INSTANCE] 已在运行，跳过"
    exit 0
fi

# 清残留空壳 screen 会话
if screen -list 2>/dev/null | grep -q "\.${INSTANCE}[[:space:]]"; then
    screen -ls 2>/dev/null | grep "\.${INSTANCE}[[:space:]]" | awk '{print $1}' | \
        while read -r sid; do screen -S "$sid" -X quit 2>/dev/null || true; done
    screen -wipe >/dev/null 2>&1 || true
fi

# 凭据检查
if [ ! -f "${WORKDIR}/.env.${INSTANCE}" ]; then
    echo "[start_bot $INSTANCE] ⚠️ 凭据文件不存在，拒绝启动"
    exit 1
fi

cd "$WORKDIR" || exit 1
# 启动时用 venv 完整路径（不用 source activate），让进程命令行带上项目路径前缀，pgrep 才能精准区分项目
screen -dmS "$INSTANCE" bash -c "${WORKDIR}/venv/bin/python ${WORKDIR}/main.py ${INSTANCE}"
echo "[start_bot $INSTANCE] 已启动"
```

### 5.2 stop_bot.sh

```bash
#!/bin/bash
# 优雅停止指定实例。用法: ./stop_bot.sh [实例名]  默认 bot1
# 前提：程序注册了 SIGTERM 信号处理，收到后优雅退出
# 多项目共存：每项目的 WORKDIR 不同，pgrep 只匹配本项目进程，不会误杀其他项目。
set -u

INSTANCE="${1:-bot1}"
WORKDIR="/root/仓库名"          # ← 改成你的实际路径

# 用本项目 venv 完整路径做 pgrep，只杀本项目进程
PID="$(pgrep -f "${WORKDIR}/venv/bin/python.*main.py ${INSTANCE}\$" || true)"
if [ -z "$PID" ]; then
    echo "[stop_bot $INSTANCE] 未找到进程，跳过"
    exit 0
fi

echo "[stop_bot $INSTANCE] 发送 SIGTERM → PID=$PID"
kill -TERM $PID

# 等待退出（最多 30s）
for i in $(seq 1 30); do
    if ! kill -0 $PID 2>/dev/null; then
        echo "[stop_bot $INSTANCE] 已退出，耗时 ${i}s"
        # 清残留空壳 screen
        screen -ls 2>/dev/null | grep "\.${INSTANCE}[[:space:]]" | awk '{print $1}' | \
            while read -r sid; do screen -S "$sid" -X quit 2>/dev/null || true; done
        screen -wipe >/dev/null 2>&1 || true
        exit 0
    fi
    sleep 1
done

echo "[stop_bot $INSTANCE] ⚠️ 30s 未退出，请手动检查（勿贸然 kill -9）"
exit 1
```

> **程序侧前提**：你的代码需要响应 SIGTERM——收到信号后设置 `running = False`，让主循环退出，最后清理资源（撤单、关连接等）。

### 5.3 部署脚本

```bash
chmod +x stop_bot.sh start_bot.sh
```

手动验证一次：

```bash
./stop_bot.sh bot1
# 确认日志里有优雅退出的记录
./start_bot.sh bot1
screen -ls   # 确认 bot1 会话出现
```

---

## 6. 定时任务（cron）

**场景**：每天某时段不想跑策略，定时停机 + 定时重启。

**关键认知**：cron 是 Linux 自带定时器，配置一次永久自动运行，不需要每天手动操作。

### 6.1 确认时区

很多云服务器的 cron 走 UTC。先用 `timedatectl` 确认。如果服务器时区是 UTC，crontab 里的小时数要写 UTC 时间：

```
北京时间 15:00 = UTC 07:00
北京时间 17:00 = UTC 09:00
```

（北京与 UTC 均无夏令时，固定 −8，一次换算永久有效。）

### 6.2 配置

```bash
crontab -e
# 首次会提示选编辑器，输入 1 选 nano 回车
```

粘贴（路径换成你的实际路径）：

```
# 北京时间 15:00 停机、17:00 重启（UTC 07:00 / 09:00）
0 7 * * * /root/仓库名/stop_bot.sh bot1 >> /root/仓库名/data/bot1/cron.log 2>&1
0 9 * * * /root/仓库名/start_bot.sh bot1 >> /root/仓库名/data/bot1/cron.log 2>&1
```

**时间格式**：`分 时 日 月 星期`。改时段只改小时和分钟两个数字。

### 6.3 维护

```bash
crontab -l               # 查看当前定时任务
cat data/bot1/cron.log   # 查看执行记录
crontab -e               # 改时段或注释掉取消
```

---

## 7. 多账户运行

同一台服务器跑多个账号，每个是完全独立进程：

- 各自 `.env.<instance>`（私钥不同）
- 各自日志目录
- 各自 screen 会话
- 各自 API 限流额度

```bash
./start_bot.sh bot1
./start_bot.sh bot2
```

不同账户的策略参数可以在各自的 `.env` 文件里设不同值。

---

## 8. 日志管理

### 8.1 推荐配置

```python
from logging.handlers import RotatingFileHandler

# 控制台：INFO（screen 里实时看）
# 文件：DEBUG（排查用），单文件 200MB，保留 5 个历史 = 最多 1.2GB
fh = RotatingFileHandler(
    "data/bot1/app.log",
    maxBytes=200 * 1024 * 1024,
    backupCount=5,
)
fh.setLevel(logging.DEBUG)
```

### 8.2 日常查看

```bash
tail -f data/bot1/app.log                              # 实时跟踪
grep -E "ERROR|WARNING" data/bot1/app.log | tail -50   # 最近错误
grep "关键词" data/bot1/app.log*                       # 含轮转历史（*.log.1/.2 等）
```

### 8.3 轮转说明

`RotatingFileHandler` 文件达到 200MB 后自动切为 `.log.1`、`.log.2`...进程内自动完成，`logging` 模块自带锁保护，多线程安全。多账户各自独立文件，无跨进程争用。

---

## 9. 安全检查清单

**每次提交前**：
- [ ] `git status` 看不到 `.env`、`.env.*`、`data/`、`*.log`
- [ ] 代码里没有硬编码私钥、API key
- [ ] `.gitignore` 含 `data/`、`*.log`、`.env`、`.env.*`

**服务器上**：
- [ ] `.env.*` 权限是 `600`
- [ ] 日志目录存在（否则写日志失败）
- [ ] `screen -ls` 确认进程在跑

---

## 10. 命令速查表

| 操作 | 命令 |
|------|------|
| **本地提交推送** | `git add 文件 && git commit -m "描述" && git push` |
| **SSH 登录** | `ssh root@IP` |
| **克隆仓库** | `git clone https://github.com/用户名/仓库名.git` |
| **服务器拉代码** | `cd /root/仓库名 && git pull` |
| **装/更新依赖** | `source venv/bin/activate && pip install -r requirements.txt` |
| **前台启动** | `python main.py bot1`（venv 激活后） |
| **脚本启动** | `./start_bot.sh bot1` |
| **脚本停止** | `./stop_bot.sh bot1` |
| **screen 列表** | `screen -ls` |
| **进入 screen** | `screen -r bot1` |
| **挂后台** | `Ctrl+A` → `D` |
| **实时日志** | `tail -f data/bot1/app.log` |
| **错误检查** | `grep -E "ERROR\|WARNING" data/bot1/app.log \| tail -50` |
| **编辑 cron** | `crontab -e` |
| **查看 cron** | `crontab -l` |
| **传凭据到服务器** | `scp .env.bot1 root@IP:/root/仓库名/` |

---

> **模板版本**：V2.0（基于 Guardian V8 踩坑经验 + Polymarket 官方文档 2026-08 最新内容）
>
> 官方文档入口：[docs.polymarket.com](https://docs.polymarket.com) — Python SDK、认证、实时数据、交易接口均在此。
